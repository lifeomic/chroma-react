import {
  CornerRightDown as FeatherCornerRightDown,
  Props,
} from 'react-feather';
import * as React from 'react';

export const CornerRightDown: React.FC<Props> = ({ ...rootProps }) => (
  <FeatherCornerRightDown data-icon="cornerrightdown" {...rootProps} />
);
