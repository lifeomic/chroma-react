import black from './black';
import gray from './grey';
import { TypeText } from './types';

const text: TypeText = {
  primary: black[900],
  secondary: black[600],
  disabled: black[400],
  hint: black[400],
  contrast: gray[200],
  dark: black[900],
};

export default text;
